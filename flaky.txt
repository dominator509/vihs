# Flaky test quarantine (TESTING.md policy, EP-007 M5).
# One test nodeid substring per line; '#' comments allowed. A quarantined
# test still RUNS every gate — its failure is non-gating (WARNING only).
# Max 5 working days with an open note in the active ExecPlan, then fixed
# or its feature is reverted. Time-dependent flakiness is fixed by
# injecting clocks, never by widening sleeps.

# EP-009 M4 open note: test_pipeline_overhead_within_budget_slack asserts
# e2e first-frame <= 725ms stage midpoints + 150ms slack = 875ms. On this
# shared box (memoryd + orchestrator + report server + cloudflared running,
# load avg ~1.7-2.8 on 6 cores) scheduling noise pushes p50 to ~885ms —
# ~10.5ms over budget. Proven pre-existing: fails identically at HEAD~1.
# Mock stage code untouched by EP-009 M4 fixes (tts.py is the real piper
# path; MockTTS is separate). Proper fix (clock injection per TESTING.md)
# is EP-005 M6 scope, deferred; quarantine expires 5 working days.
pod/tests/test_latency_harness.py::test_pipeline_overhead_within_budget_slack
