# deploy/runpod — provider templates + volume layout

EP-009 will add the RunPod provider driver, pod templates, and the network
volume layout doc here. This directory exists to satisfy the ARCHITECTURE.md
§2 repository contract from EP-001.
